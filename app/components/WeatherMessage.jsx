var React = require('react');

var WeatherMessage = React.createClass({
    render: function () {
        return (
            <h4>It is rather cool in Hades</h4>
        );
    }
});

module.exports = WeatherMessage;